schema {
    query: Query
    mutation: Mutation
}

type Query {
    userByName(name: String!): User
    userByEmail(email: String!): User
}

type Mutation {
    createUser(name: String!, email: String!): User
}